# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build the library in `packages/react-liquid-glass/src/` and the documentation/demo app in `apps/docs/src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact. Sites handoff uses `npm run build:sites` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Component library and documentation site

- The user requested an npm-workspace monorepo with independently owned library and documentation builds. The docs app consumes only public package APIs; no relative imports into library implementation and no demo imports in the library.
- The docs site has a home, searchable component catalog, individual preview/usage/API pages, a material Playground that affects every component, and several working creative mini-apps. This supersedes the former single-page/no-header composition constraints, but preserves the restrained monochrome style, Fontsource typography and approved material/gesture implementations.
- Common components must compose the project-owned liquid-glass and apple-motion cores. Preserve accessible native semantics and real controlled state. Playground changes must reach the renderer; never substitute CSS blur or decorative replicas.
- Keep Git-push deployment working for the existing Vercel project after the workspace migration. Do not reintroduce the deleted architecture document or QR specimen.
- New library UI glass stays clean: fine directional contours, darker at the sides and lighter at the top/bottom, with a narrow inset reflection and no broad black glow. Calibrate compact surfaces separately from the approved original menu within the same optical renderer. Large dropdowns/popovers gain continuous background frost as their live body grows; small controls and tooltips stay clear. Extend the existing HDR presenter to fine reflections as well as pointer light, with an SDR fallback and no idle render loop. These are reference-informed project settings, not measured iOS 27 values.

## Core refactor

- Refactor on `refactor`: split the reusable cores into `liquid-glass` (material, SDF fusion, optical deformation, substrates and GPU lifecycle) and `apple-motion` (springs, velocity response, gestures and interruptible trajectories). Controls compose both; neither core imports Demo code or the other core's implementation.
- The current glass material and accepted interactions are the visual baseline. Preserve the shader, optical calibration, geometry and normal menu trajectories. Improve motion through measured stability/continuity and reuse, not a new visual style. Project motion calibrations are not claimed Apple-native constants.

## Dezin Glass fidelity

- Media is video-only. The user removed the QR Code section: do not mount or lazy-import QrGlassDemo in the page, restore its labels/loading stage, or leave an empty panel. Preserve the video and other glass interactions. Retained QR reference code does not belong to the visible Demo; this supersedes older instructions that require a QR specimen.
- Shared Liquid foundation on main: the user approved promoting the unified Liquid implementation to `main` and removing the temporary foundation branch. All Demo glass materials (Hero, Switch, Slider, Segmented, action button, menu, QR, Video, Experiment) use this foundation. This supersedes older material/renderer freezes and the old internal-only LiquidGlassCanvas restriction, not accepted control anatomy, pointer/keyboard behavior, motion trajectories, locale/theme or accessibility. Keep the approved Liquid menu appearance and closing absorption unchanged. Reuse one project-owned optical shader and preset, expose a reusable foundation, and optimize shared small-surface GPU contexts, dirty textures, video-frame scheduling, offscreen pause and idle rendering. Retain the legacy Glass API for compatibility/comparison; it must not render any Demo surface. Experiment must expose real new-renderer parameters and its live optical field, not obsolete SVG-map controls that do nothing. Document the bounded DOM redraw adapter honestly: it is not browser-native DOM capture, a universal backdrop, or proof of performance/native-iOS parity.
- Foundation control refinement: Switch and Slider thumbs remain opaque white at rest and reveal glass only during activation, continuously through the same material rather than a renderer swap. Tabs must reveal each icon's actual colors through the lens; positioned native gray content must not paint above the optical canvas. End glass in the low-amplitude geometric tail, overlapping travel/recoil recovery instead of serially waiting for every spring to finish. Preserve the velocity-driven stretch/compression, trailing bias and single contact recoil; menu fusion is not a new segmented gesture. Broad background blur needs prefiltered, continuous sampling rather than separated duplicate strokes; retain CSS-pixel blur strength and cache prefilter work with source revisions. Other accepted sections, edge highlights and menu motion stay unchanged.
- Control optics need their own scale calibration within the shared Liquid material: keep refraction in CSS pixels, independent of source padding, Slider length, or the number of visible Tabs. Keep curvature below the small thumb's hemisphere clamp, a clear central area with a narrow refracting band, and restrained dispersion; do not inherit the large menu's optical scale or retune its approved material. The Slider's refracted active fill must have its own fully rounded endcap, not a rectangular fill merely clipped by the whole track. Preserve white rest, colored Tabs ink, and accepted gesture/settling physics. These control settings are project calibration, not measured native iOS 27 parameters.
- Liquid close timing refinement supersedes the earlier 420ms close target: use 380ms for a fully opened menu. Outside dismissal during opening must scale the return clock with the current body size (209–380ms), not restart a full-duration close for a small partial body. Keep the bounded momentum brake and live velocity, synchronize geometry, button, material, content attenuation and focus restoration to the chosen clock, and preserve the approved normal-close fusion/impact trajectory, opening timing, material and other controls.

- Exact source fidelity is blocking: do not substitute approximate optics, component dimensions, parameters, motion, or styles.
- Reject visible aliasing, dark seams, stale filters, and parameter changes that stall or crash the page.
- Validate against the captured source at matching desktop/mobile viewports before handoff.
- The Switch, Slider, and Toggle demos inside "A family of components" are user-approved and frozen. Preserve their component anatomy, optics, dimensions, parameters, motion, and styling. The QR and video demos are not approved and must match the source WebGL implementations.
- Updated component acceptance criteria supersede the prior freeze: Switch thumb expands into pure glass while held and magnifies the track fill; Slider grabs the pointer immediately, keeps its value readable through glass, and squashes by handle velocity; Toggle pill spring-follows the active item, stretches in motion, magnifies synchronized content, and reveals brand color from gray icons under the glass.
- For Switch and Slider, the production Dezin page implementation is the final authority. Do not translate behavior descriptions into invented UI such as an added numeric value layer or substitute approximate spring/tween state machines.
- Video glass-button circle edges must remain smoothly antialiased: SDF coverage must attenuate displacement, blur, adaptive tint, and specular together; never apply full-strength displacement inside only the partial-coverage edge band.
- The Demo page is Chinese-only and brand-neutral: remove visible Dezin names, logos, encoded URLs, app imagery, and English UI copy. The segmented control must support mouse press-drag switching across visible tabs while preserving click and keyboard behavior.
- Segmented press-drag switching must keep the ordinary pointer cursor; never show grab/grabbing cursors. Keep the QR center icon geometrically centered through every 3D rotation state.
- Segmented appearance follows the current iOS 27 control model: the selected pill is a very light neutral transparent-gray highlight at rest, never a colored fill, border, or shadow; its selected icon keeps that item's own colors. Non-selected hover uses an even lighter neutral background. Only press/drag morphs the pill into glass. A stationary press reaches the maximum vertical height, about 148% of the tab group. As movement speed increases, the glass must stretch horizontally and compress vertically; as speed falls, its height returns toward the stationary maximum. At maximum speed it must still remain slightly taller than the group, around 112%. On release, immediately spring the fixed vertical boost back to the tab height alongside travel deceleration; never hold it until the opacity fade. Pointer speed must visibly drive strong expansion and horizontal stretch. On fast travel and braking, preserve volume-like squash/stretch: horizontal elongation pairs with vertical compression, target overshoot then produces horizontal recoil and vertical bounce. At high-speed arrival, the leading edge must feel as if it hits the destination: keep most velocity stretch trailing behind it, soften forward center overshoot, and retain only a short rebound instead of letting the full symmetric lens spill far beyond the first or last tab. After the first contact, rebound velocity must not re-excite fresh deformation; allow one clean recoil, then damp width, height, and optical-center bias smoothly to rest without sticky micro-oscillations. A click transition must visibly run current-position expansion -> elastic glass travel to the clicked tab while shrinking during the second half -> collapse to the gray highlight. Travel and collapse must overlap and preserve one continuous travel velocity; never jump, restart the travel spring, or wait until arrival before shrinking. Use analytical mass/stiffness/damping springs that preserve velocity across retargeting and carry gesture release velocity into settling; never restart a hand-written spring at zero velocity. Keep travel near Apple's documented 0.5s, moderate-bounce physical range rather than a fast easing-like response. Keep glass visibility separate from shape deformation and begin the final optical attenuation once shape collapse has settled and positional motion has entered its low-amplitude tail, before the travel spring is fully at rest. Freeze significant lens geometry changes first. Treat the transparent-gray selection as the glass's persistent base material: place it fully underneath before the final stage, then attenuate only the refraction/chroma/specular optical layer; never crossfade by making glass disappear while separately making the base material appear. Drive both layers directly with compositor-bound MotionValues, not a per-frame CSS custom property. During the optical attenuation, selected text and icon colors must match across the interactive base, glass base, and refracted layer; never flash black -> gray -> black. During drag, the lens must track the pointer directly and use nearest-segment hit testing so gaps and restarted springs never make it feel sticky. At both edges, the destination anchor remains exactly on the first/last tab center; only live velocity stretch may bias the rendered optical center backward so the leading edge does not spill unrealistically past the track.
- After first contact, compress residual center displacement in both directions so the remaining energy reads primarily as shape recoil, never as a slow positional crawl back to the selected tab.
- A captured mouse pointer is not an active drag until it crosses the movement threshold. A stationary long press must settle once into a stable expanded-glass state and must not keep re-exciting velocity deformation or twitching while held.
- Restore the stationary-hold Q-bounce with an explicit one-shot impact script (arrival impulse plus phase-specific stiffness/damping), not by reopening continuous velocity feedback. Short-click behavior stays independent and unchanged.
- The next full-page redesign must replace the source article shell, including structure, palette, typography, and layout. Use Fontsource packages for all interface fonts and preserve the approved glass interaction implementations unchanged.
- For future page design, do not generate image mockups or image references first. Implement a simple, clean layout directly and put the visual richness into polished motion.
- Keep Demo-page composition in `src/App.tsx`, shared presentation primitives in `src/demo/`, self-contained examples in `src/demos/`, and page styling split across `src/styles/base.css`, `page.css`, and `demos.css`. Page redesigns must not alter approved library-control or media-renderer implementations.
- Keep the Demo visually extreme-minimal: section titles, essential action hints, and the interactive surfaces only. Do not add explanatory card copy, hero statistics, decorative circles, radial blobs, or a glassy top navigation.
- The Demo hero must read like a refined article: a short title and one-line introduction followed by one full-width hero visual. Never use a left-right split hero.
- Keep the Demo palette strictly monochrome. Use only black, white, and neutral grays for page chrome, typography, demo surfaces, and newly authored examples.
- Avoid landing-page templates in the Demo: no sticky section navigation, theme toggle, oversized marketing headline, repeated rounded cards, global entrance fades, or decorative grid behind every component. The single hero glass demo is the visual signature; everything else should read as quiet editorial specimens separated by whitespace and hairlines.
- Do not add self-explanatory hero slogans, generic value propositions, interaction hints, or diagram captions to the Demo. If the interaction is visually obvious, leave it unlabeled; retain only section and component names needed for navigation.
- The Demo has no top header. Its first element is the interactive hero glass over a real monochrome photograph, with no decorative icon. Parameter changes in the experiment must never spawn emoji or decorative particles; its lens has no white outline and only a light neutral shadow.
- The experiment includes an off-by-default “底图” GlassSwitch that reveals the same monochrome photograph used by the hero. The hero and segmented-control glass use restrained chromatic dispersion (`chromaAmount: 0.24`); do not alter segmented motion physics when tuning this optical value.
- Animated map regeneration must treat numeric geometry values such as `depth` as live dependencies and commit the new `feImage` URL before forcing filter invalidation. Generated map alpha is the rounded-lens coverage mask; final blur/refraction compositing must use that alpha so rounded corners never inherit rectangular blur. Experiment highlight controls require a nonzero adjustable `specularStrength`.
- The experiment keeps core controls visible and places all remaining LensParams plus zoom, render resolution, region transform, and shadow intensity inside a collapsed native “其他参数” details section. When `pauseOffscreen` clears a filter, visibility restoration must invalidate cached position/scale before reapplying the filter; returning onscreen must never require pointer input.
- The experiment's specular highlight angle covers the full 0–360° range at 1° increments.
- The Demo supports persisted light/dark mode through a plain-text toggle at the bottom-left of the footer. Animated map generation must recreate its generator at the requested `mapSize` before rendering; the experiment exposes 64–1024 so low-resolution quality differences are observable, while making clear through behavior that resolution affects sampling quality rather than lens geometry.
- Switch and Slider provide a `size="small"` variant (`52×20` and `120×16`) used by every experiment control; default geometry remains unchanged. Dark Switch uses a dedicated neutral enabled-track token rather than the global primary color. The action button is icon-free and uses a neutral dark glass tint.
- There is no standalone “交互” section. The action button is the fourth specimen inside “控件”.
- The collapsed advanced experiment section lazily mounts its small controls only while open. Small Switch/Slider surfaces use the same bounded 2× Canvas resolution as default controls: never upscale a 1× bitmap, which visibly blurs the white thumb and glass rim. This supersedes the old SVG map-size/filter-resolution limits; preserve the existing blur, optics, geometry and motion.
- Performance contract: the video renderer pauses and cancels frame work offscreen, follows `requestVideoFrameCallback` when available, and caps render scale at 2.5. Small controls share one GPU context, reuse source textures, pause offscreen and stop drawing at rest; opaque resting thumbs skip invisible frost work. Parameter rows are memoized with a stable update callback.
- Video shader pixels outside active glass return the raw video before frosted sampling. Circle and bar masks use the source `fwidth` AA at the source-matched 1.25×DPR render scale (capped at 2.5). Video buttons use the source spring constants (side 1000/40/1.5, play 500/32/1), synchronize DOM and lens scale, and hover to 1.045.
- Video seeking marks the texture dirty immediately and again on `seeked`, so paused scrubbing always refreshes the canvas. Match the source seek-bar behavior exactly: no vertical press expansion; only out-of-bounds horizontal rubber stretch, passed through a 550/35/1 spring. Canvas lens width/center and DOM bar scale/origin use the same spring value, then return to zero on release.
- Video seeking shares the pointer-release fallback used by Switch and Slider. Pointer capture loss, off-viewport release, window blur, or page hiding must always clear drag state, spring horizontal stretch back to zero, and restore prior playback state.
- Video DOM controls and WebGL lens geometry must share the source `767px` responsive breakpoint: buttons scale to 75% and bar margins to 50% together. Rewind/forward are 15-second actions, and paused video must redraw immediately after resize so icon lenses and the progress bar never retain stale desktop geometry.
- Slider track clicks spring from the current thumb position to the snapped target value. Crossing a 3px pointer threshold cancels that click spring and hands control to direct dragging from the current animated position without a jump.
- Switch and Slider must always collapse out of glass interaction state when pointer capture is lost, the pointer is released or cancelled outside the component, the window blurs, or the document becomes hidden.
- When a segmented click immediately turns into a drag, cancel the click travel spring and use a short physical catch-up spring from the current glass center toward the live pointer. Keep the target updated while the pointer moves, then transition naturally into direct tracking; never preserve the old-position offset or hard-snap to the pointer.
- Button, QR, and experiment grid backgrounds keep their pattern phase centered so opposite edges have symmetrical partial cells.
- Runtime component styles use the project-owned `dg-*` namespace (`dg-switch`, `dg-slider`, `dg-tabs`, `dg-hero`, `dg-qr`, `dg-video`) plus `data-dg-glass-surface`; never reintroduce source-site class names or unnamespaced component CSS variables.
- The QR icon's 3D side wall stays vertically inset within the rounded front/back faces; never scale or translate it beyond the face bounds, which creates a bottom spike during rotation.
- The Demo supports persisted Chinese/English locales across visible and ARIA copy and updates `html.lang`. Footer left contains a Sun/Moon icon-only theme button beside a compact `中`/`EN` language switch.
- The footer language button displays the current locale (`中` for Chinese, `EN` for English); its ARIA label describes the target locale.
- Core `Glass` and `GlassCanvas` ship without CSS. Optional Switch/Slider/Tabs styles live in the standalone `controls.css` export with self-contained fallback tokens; the Demo imports that file explicitly.
- The Demo defaults to English when no locale has been saved; a persisted user choice still wins. Keep the browser title `React Liquid Glass`.
- External visual-effect libraries are reference sources only unless the user explicitly approves a dependency. For Liquid/Gooey work, study the source and implement a project-owned `dg-*` renderer; the merged neck must participate in the same SDF, refraction, chroma, and specular pipeline instead of being a translucent silhouette layered over separate glass circles. Keep this work isolated from the approved controls and media renderers.
- Glass2 is a Glass-effect project: every newly authored stable visible glass component must compose the existing project-owned `Glass` or established substrate-specific `GlassCanvas`/media renderer. New code may add interaction, layout, or geometry, but must never introduce an unrelated approximate optical material. The Liquid menu and icon button use the internal, non-exported `LiquidGlassCanvas` compositor for their entire visible lifecycle, so one SDF and one material render rest, press, morph, merged neck, and settle without a renderer handoff. Its WebGL material mirrors core Glass's per-blob spherical-cap gradient, inner rounded-rect depth falloff, exact `tanh(√π·x)` erf approximation, nine-tap frost, brightness, tint, chroma, local-coordinate rotated glow, and edge-highlight parameters. `zoom` multiplies displacement strength exactly as core Glass does; it must never magnify the source texture coordinates. Never reintroduce a core/Canvas opacity handoff, an external gooey dependency, or a tail droplet.
- Liquid dragging has no trailing droplet or tongue. Keep velocity response confined to volume-preserving squash/stretch of the primary body; liquid merging happens only between intentional interactive blobs.
- The Liquid specimen is a single iOS-style menu button that physically morphs into one tall rounded translucent menu panel with section headings, rows, icons, divider, and a real scroll region; it must not become a chain of circular action blobs. The supplied iOS screenshot defines structure only, never its dark color treatment: material, text, and states must use the Demo's adaptive monochrome light/dark tokens. Its substrate uses the QR demo's centered 72px neutral square-grid geometry with enough contrast to make refraction readable, never QR modules or an actual QR code.
- Theme adaptation must never flatten the Liquid specimen into an opaque white or dark card. Its circle-to-panel transition must retain the project's signature live refraction, restrained chromatic dispersion, frosted sampling, directional bright/dark rim, and SDF-bound shadow throughout the full liquid morph; do not add a separate static DOM outline that detaches from the liquid surface.
- Liquid menu motion fidelity is primarily about the liquid contour's shape, size, and position throughout both opening and closing, not just optical tuning or content fades. Keep different trajectories near 380ms open and 420ms close: opening contracts, grows a round body into a fat capsule (width develops before full height), then relaxes the corners into the panel with only slight overshoot. Front-load the main expansion and center travel, leaving the final portion for contour recovery; do not keep the body pinned to its lower-right corner until it reaches full size. Closing must retain the user-preferred two-body interaction: the icon button appears while the menu is still shrinking, the two intentional bodies join through a liquid neck, and the button absorbs the menu with a visible directional impact and restrained recovery. A unified SDF compositor means continuous material across both bodies and their neck, not a requirement to use only one blob. Never simplify closing into a lone panel shrinking into a circle; that removes the approved fusion and physical-impact cues. The supplied four-frame closing sequence refines that interaction: content defocuses and fades while the panel is still large, then the rounded mass follows a small anchored head through a narrow neck, becomes an unequal two-lobed body with a readable ellipsis, and is absorbed before one short recovery. Keep this relative pull directed toward the existing lower-right trigger; the reference's upper-right trigger is not a request to relocate the specimen. Menu half-extents end at 1px inside the button; the button grows through 16px and 33px to a 34.6px peak half-size, then returns to 34px with at most 2px of shared directional displacement. Keep the neck visible around the middle of the close and the two-lobed stage near 70%, rather than burying the button inside an overlapping panel. Absorption velocity drives the button's directional squash/stretch without re-exciting recoil. The button rest center remains 38px inward from the panel's right and bottom edges. Shape-keyframe tangents must be continuous; press/swell/recoil reversals reach zero velocity, while middle knots never stop. Derive SDF stretch from actual geometry velocity, not a second preset velocity timeline. Mid-morph reversal retains live shape and velocity and skips the old anticipation pose. These are project-tuned trajectories informed by iOS 27 footage, not claimed Apple spring constants or measured native durations; selection dismissal and outside-click cancellation remain distinct reference cases. The SDF deformation must reconstruct world coordinates after directional squash/stretch (`direction * along + tangent * across`); rotated local coordinates alone rotate a nonsquare lens abruptly. Lock trigger press/release handlers for the full morph, preserve press feedback in the same Canvas material, and bind the icon to the returning button's center, not the shrinking panel's center. Opening content reveal lasts 340ms but starts in the early capsule rather than after a 197ms delay. Closing retains a 240ms opacity tail, but front-loads defocus into the first 80ms and most content attenuation into the first 125ms so the remaining glass mass reads clearly. This early defocus is closing-only and must unwind continuously on reversal; normal opening clarity and the background frost are unchanged. Content translation, scale, directional deformation, and rounded clipping follow the live panel geometry through both directions; never reveal a stationary full-size menu behind a shrinking mask.
- Liquid menu density is compact: scroll padding must be uniform on all four sides—`14px` desktop and `10px` mobile. Sort rows have a fixed `64px` height before and after selection. Item corners use ordinary circular radii—`31px` desktop and `30px` mobile—never elliptical slash radii or `squircle`. Native `corner-shape: squircle` applies only to the Liquid stage, panel, and scrollbar thumb; the Canvas remains a rounded SDF and the icon trigger remains a true `50%` circle with `corner-shape: round`. Do not apply this change to frozen components outside the Liquid specimen.
- Liquid material uses visible monochrome-grid dispersion (`chromaAmount: 0.55`), `specularStrength: 0.72`, and a broad glow (`glowStrength: 0.30`, `glowSpread: 0.72`). Background frost remains perceptually continuous through morphs: derive blur monotonically from lens geometry between the closed `0.5` and open `1.6` endpoints, preserve per-channel chromatic sampling through that frost, and composite specular, brightness, then tint in core Glass order. Never add a separate all-around inset rim; it both changes the material language and duplicates SDF work. Blend depth, tint, and zoom continuously between the button and panel values from that same geometry progress so press feedback survives without creating a second material state. Opening contraction and closing swell/impact reversals must reach zero velocity before reversing, while the middle shrink segments retain compatible slopes; keyframe segment boundaries must not read as dropped frames. Its SDF shadow uses an 18px downward offset, 26px Gaussian deviation, and a soft signed-distance falloff at `shadowStrength: 0.11`; never leave a solid offset umbra or a bright detached outer ring.
- Use the existing Video WebGL2 renderer as Liquid's reference for background-adaptive highlights and SDF coverage AA: highlights brighten dark substrates and darken bright substrates with the same luminance response. Preserve Liquid's core-Glass displacement, chromatic frost, and merged geometry. Keep MSAA and the unused depth buffer disabled, retain 2× render resolution, and batch Canvas drawing into Motion's render phase so the lens and DOM contents share the same animation frame.
- The user-supplied iOS 27 capsule and menu close-ups define Liquid's edge structure: a continuous fine gray outer contour, darker at the left/right and lighter but still present at the top/bottom, plus a subtle, narrow inset reflection confined to the top/bottom. The menu reference supersedes the earlier request for stronger luminous lobes: never let the bright crest become a thick white strip or extend down the sidewalls. Derive edge orientation from screen derivatives of the existing merged SDF distance, not radial position within a tall panel, and smoothly gate the reflection to the upper/lower arcs. The dark contour and bright crest need separate distance profiles and independent directional strengths; preserve a narrow bright rise followed by a short, softer inward falloff without erasing the outer contour. Both profiles must use the existing live merged SDF distance and coverage throughout rest, press, opening, fusion, and closing, without additional SDF-normal sampling, a DOM outline, or another render pass. Keep the broad glow's Video-style adaptive response separate from these fine edge profiles. Calibrate against the supplied close-ups on a neutral light substrate; screenshot pixel widths are not claimed native iOS point measurements. Edge tuning must not change blur, dispersion, shadow, or motion without a separate request.
- Liquid's current top/bottom dark contour and inset highlights are user-approved. Further side-contour refinement must leave them unchanged: slightly soften and narrow only the left/right dark line, smoothly blending through the corners using the existing contour orientation. Do not reduce the shared edge width or strength globally, which would also change the approved upper/lower edges.
- Liquid mid-morph retargeting must brake outgoing or excessively fast momentum in a short bounded segment (at most 40ms), preserving the initial velocity. Do not stretch that initial tangent across the full new transition: front-loaded opening then balloons far beyond the panel during rapid open/close reversals.
- Liquid's opening/closing content must participate in the glass optics: the supplied moving-menu frame shows edge-local stretching and bending of text, icons, and dividers, with softer central content that clears as the shape settles. Reuse the live merged SDF/displacement for this foreground ink, not a uniform DOM blur alone. Preserve the existing shape trajectories and approved rim/background material while tuning content optics. Capture the actual localized/scrolled menu at transition boundaries, refresh for live content/scroll/focus changes, and reuse its premultiplied texture during motion; do not rasterize DOM every frame or duplicate the menu layout. Prefilter text to avoid discrete multi-image stroke ghosts. Return to native interactive DOM only at the neutral optical endpoint, without overlapping ink layers or a visible typography/clarity jump; retain native scrolling, keyboard focus, and reduced-motion behavior.

## Component library refinement (2026-09-06)

- Keep website copy concise: component names, useful API/keyboard guidance, minimal introduction. Remove repeated slogans and generic explanatory copy.
- All anchored overlays (Popover, Select, DropdownMenu, Tooltip and the catalog material panel) use the native Popover top layer and the shared liquid-menu capsule growth / two-body absorption trajectories. Trigger and popup share one project-owned SDF material and an intentional fusion neck. Preserve exit animation before native dismissal, keyboard navigation, focus restoration and reduced motion.
- Accordion animates its height and keeps the header and expanded content inside one live glass surface. Alerts and toasts have physical entrance motion; toast dismissal completes before removal.
- The component catalog has a floating, collapsible material configurator. Default/reset restores each component's own calibration; edited parameters override every preview. Interaction state still owns the opaque white resting Switch/Slider endpoint, and small control canvases retain 2x sampling.
- Switch has a persistent neutral base in both themes. Segmented and Tabs containers use the shared glass renderer. Their selection motion is now restrained: one compact elastic travel/recoil with modest volume-preserving deformation and no large zoom or trailing stretch. This supersedes earlier exaggerated 148% expansion and long-travel calibrations; preserve direct dragging, velocity across retargeting, colored selected ink and release fallbacks.

- Use Liquid Glass selectively in the docs site: keep specimens, useful interactive tools and the floating material panel. The wordmark, theme/language/GitHub controls, sidebar including its search, breadcrumbs, footer, section links, component labels and code blocks stay plain. Do not wrap every link or content container in glass. Preserve native semantics, concise copy and the restrained monochrome layout. This supersedes the earlier request to glass every site control.
- Provide a reusable GlassButtonGroup with horizontal/vertical layouts, one continuous shared optical surface, independent native button/link actions, and keyboard focus navigation that skips disabled items. Use it in the site itself and include it in the catalog, standalone usage docs and Playground.
- Define light/dark color schemes in CSS so production-transformed `light-dark()` colors resolve correctly, and verify Switch tracks in both themes from the production build. Popover closing should gather immediately, shrink continuously and retain the canvas frame through absorption; restore native trigger ink before dismissing the top layer. Trigger mirrors must retain computed typography longhands, and captured popup ink must include ancestor opacity and the opening focus state so activation and optical settling do not shift text or change content styling. Select uses Lucide icons, not text glyphs.
- Contact feedback should feel like gripping real glass: illuminate the actual press location, resist directional pulling while the opposite material point stays anchored, and return elastically on release. Contour, optical coordinates and native foreground use the same affine deformation; no detached CSS glow, trailing droplet or click after a drag. Existing Slider/Switch/Segmented gestures and popup morphs retain their own mechanics and add contact light. HDR output is capability-gated and uses the same SDF mask; ordinary screens keep a local SDR highlight. Preserve cancellation, scrolling/editing, reduced motion, velocity on interruption, shared devices and idle/offscreen suspension.
- Contact deformation must remain subtle even on a long drag: bound resisted pull to 6.5% of the smaller dimension, at most 4 CSS pixels. Keep the initial shape anchor separate from the moving light position so the highlight follows the pointer without moving the pin. Do not exaggerate the pull.
- Floating glass must show the actual content underneath it, not a stock gray source. Popovers outside an explicit stage use the bounded page-region adapter with self-exclusion, scroll/DOM/canvas invalidation and idle suspension. Basic surfaces on a flat page inherit that page's color. Reuse the optical core and state the adapter's limits honestly; it is not universal browser-native DOM capture.
- The whole docs page uses a faint, neutral diagonal hatch in both themes. Keep text readable and the existing specimen substrates intact. The page pattern must participate in glass refraction and stay aligned with the visible CSS background; never add texture only outside the glass.
- Press illumination has a slightly broader footprint, while the top/bottom fine reflection gains HDR brightness without widening into a white strip. Keep the SDR fallback and all existing SDF/ink occlusion; actual HDR display capability is required for extended brightness.
- Focus uses a soft light halo or a quiet menu-row fill, not a solid black outline. Preserve visible keyboard focus, native focus restoration and the system Highlight outline in forced-colors mode.
- The reusable Popover family closes in 280ms at full size, proportionally faster when interrupted early. Preserve the fusion neck, one short recoil, continuous retargeting and stable trigger ink.
- Floating trigger and panel refraction must be calibrated per body, independent of the retained canvas frame. Reserve the full soft-shadow extent. Scroll updates must be coalesced, avoid cloning unchanged trigger ink, skip unrelated/offscreen work and stop at rest. Shared surfaces retain a GPU drawing buffer and copy only their own SDR/HDR viewport; do not resize it for every small control.
- Mobile Safari is a primary acceptance target, especially the material panel's first opening and its backdrop while scrolling or resizing the visual viewport. Desktop Chrome or mobile viewport emulation alone is not device validation. Preserve the shared optical material and HDR instead of applying browser-name-based visual downgrades.
- Large viewport popovers present their WebGL canvas directly, while embedded specimens and small controls share a device. Retain blur texture storage through continuous morphs and measured control resources while a native popover is hidden; stop their rendering until visible. Compile the HDR pipeline asynchronously, and sample the SDR optical base without copying the additive HDR display layer back into the 8-bit backdrop.
- Keep popup source bounds in viewport coordinates and convert DOM placement through the actual top-layer containing-block origin. Listen to visual-viewport scroll/resize as well as page events so address-bar and keyboard changes do not separate the glass, trigger ink and underlying content.
- Input and Textarea focus must not add borders, outlines or change shadows in ordinary color modes; retain the text caret and system forced-colors focus. Textarea has no resize handle. Button-group inner corners follow the outer radius minus its padding.
- Every UI glass surface reads the visible scene through the shared bounded backdrop adapter, including demos inside GlassStage. Never retain a removed/hidden substrate as a private source or infer a two-axis grid from an arbitrary gradient. Composite control ink/track sources remain separate from the shared backdrop and must not capture themselves.
- Component previews share the Liquid Button's quiet fine 54px grid, drawn once by GlassStage. Liquid Button and Liquid Menu have no duplicate local grid. Preserve popup geometry/motion; clip trigger ink where the live expanding panel overlaps it.
- Keep one Button and one Tabs entry in the catalog and Playground; redirect the former Liquid Button and Segmented URLs. Button uses the former Liquid Button's 13px, 650-weight typography. Tabs previews contain only the control, with no changing caption or content height.
- Prism's existing five optical values are the default for ordinary glass UI. Large popups retain size-adaptive frost; calibrated small control lenses and opaque white resting thumbs stay intact. HDR is enabled by default and can be disabled per material or LiquidGlass instance. Reduce only the HDR edge lift to 0.26 of its prior gain; leave SDR edges and contact light unchanged.
- Morph Menu is the original Liquid Menu with a smaller layout and renamed entry. Preserve its four sorting options, eight filters, scroll region, content refraction, material transitions and original two-body absorption; do not substitute a generic Dropdown. Scale its geometry and content together. Dropdown Menu keeps its trigger visible.
- Dialog/Sheet have no visible mask or page dimming. Reuse the Popover's stable canvas for the real trigger geometry, traveling body and fusion neck; never leave a second glass trigger below the modal. Keep native focus containment, Escape, actual click origins, interruptible travel, large-area frost, and one restrained absorption recovery.
- Input/Textarea use larger corners (22px/24px) without focus border or shadow changes. Pressed glass grows slightly (about 2.5%) instead of contracting; preserve subtle anchored dragging and moving contact light.
- Material inspectors have stronger frost (18 CSS pixels at full size); More parameters is a plain, lazy native disclosure. The homepage Playground specimen uses different controls from The Collection.
- Toggle state colors and icon fill transition smoothly. Toast creation/removal animates its occupied height so neighboring controls move continuously.
